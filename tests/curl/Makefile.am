## Process this file with automake to produce Makefile.in

if HAVE_CURL

BASE=$abs_top_srcdir

if HAVE_JSON
TESTS = check_rabbitmq.t \
		check_buildbot_slave.t
endif

endif

EXTRA_DIST = check_rabbitmq.t check_buildbot_slave.t
## vim: set ts=4 sw=4 syn=automake :
